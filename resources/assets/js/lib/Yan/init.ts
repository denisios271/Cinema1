import * as $ from 'jquery';
import Preloader from './Preloader';
import CircleType from 'circletype'; //those need to be included only on aboute-us page
import player from './player';

const timeInitialized = performance.now();

export default () => {
    // $(window).ready(() => {
    //     player();
    //     Preloader.start(timeInitialized);
    // });
}